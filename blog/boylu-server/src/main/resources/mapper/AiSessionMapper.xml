<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boylu.mapper.AiSessionMapper">

    <select id="selectSessionPage" resultType="com.boylu.vo.ai.AiSessionListVo">
        select
            id,
            title,
            model,
            last_message_at as lastMessageAt,
            create_time as createTime
        from ai_session
        where user_id = #{userId}
        order by ifnull(last_message_at, create_time) desc, id desc
    </select>
</mapper>
