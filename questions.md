# 项目问题清单与处理建议

更新时间：2026-05-17

## P0：Notion 自动同步会覆盖已有文章内容

- 现象：服务器日志显示，2026-05-17 04:20 自动同步文章 `392` 时，Notion 返回“没有可导入的正文内容”，系统仍执行了 `UPDATE sys_article`，导致原文章正文、摘要、封面被空内容覆盖。
- 风险：只要 Notion API 某次读取不到正文，已有博客文章就可能被自动同步清空。
- 证据：日志中出现 `UPDATE sys_article`，写入内容为 `<p>Notion 页面没有可导入的正文内容。</p>`，随后文章 `392` 的 `cover` 和 `summary` 变为空。
- 当前处理：
  - 自动同步和手动同步在更新已有文章前都会检查导入结果。
  - 如果 `importedBlocks <= 0` 或正文为空，直接抛错并保留旧文章内容，不再写入占位正文。
  - Notion 表格、任务列表、删除线、附件、PDF、视频、嵌入链接、公式等块已补充基础导入格式。
- 剩余建议：
  - 增加 Notion 同步记录表或日志页面，记录成功、失败、跳过、覆盖字段数量。
  - 同步前可进一步做旧正文、封面、摘要快照，方便人工回滚历史误覆盖内容。

## P1：Notion 页面读取曾出现连接拒绝

- 现象：2026-05-16 18:50-18:52 多次出现 `Notion 页面读取失败：Connection refused`，连续重试 3 次后失败。
- 当前处理：已改为 JDK 直连 + Hutool 备用通道，重试次数从 3 次提高到 4 次，并使用 `Proxy.NO_PROXY`。
- 剩余风险：
  - Notion 外部网络仍可能偶发失败。
  - 当前失败后只抛错，没有把失败任务记录到后台可查看的同步日志。
- 建议：
  - 增加 Notion 同步记录表或日志页面，记录成功、失败、跳过、覆盖字段数量。
  - 自动同步失败不应影响已有文章。

## P1：Baidu 推送存在 SSL 握手错误

- 现象：服务器日志显示 `Baidu push failed`，错误为 `No subject alternative DNS name matching data.zz.baidu.com found`。
- 风险：文章发布后百度主动推送可能失败，但不影响文章保存。
- 建议：
  - 检查服务器 DNS/代理是否把 `data.zz.baidu.com` 解析到了证书不匹配的目标。
  - 增加百度推送失败降级：记录失败但不刷屏，不影响发布流程。

## P1：`ip2region.xdb` 读取权限异常

- 现象：服务器日志出现 `failed to load content from ip2region.xdb: AccessDeniedException: ip2region.xdb`。
- 风险：IP 归属地解析失败，访问日志或登录记录可能显示未知位置。
- 建议：
  - 确认 `ip2region.xdb` 实际路径。
  - 给运行用户 `www-data` 读取权限，或把数据库文件放到应用可读目录。

## P2：`questions.md` 之前存在编码乱码

- 现象：原 `questions.md` 大量中文显示为乱码。
- 当前处理：已重写为 UTF-8 可读版。
- 建议：后续统一使用 UTF-8 无 BOM 保存 Markdown、Vue、Java、SQL 文档。

## P2：文章封面上传已修，但仍需线上复测

- 当前处理：
  - 上传组件成功后强制切换为 `success`，避免 100% 圆圈卡住。
  - 文章封面上传改为先保存原图并立即返回，不再同步生成多尺寸封面。
- 剩余验证：
  - 后台编辑文章上传封面后，封面卡片应立即显示图片。
  - 保存文章后，前台首页和文章详情应显示新封面。

## P2：Notion 图片本地化需要状态可视化

- 当前策略：导入时先保留原链接，后台异步下载图片到本地。
- 问题：用户目前看不到图片本地化是否完成、失败了哪些图片、失败原因是什么。
- 建议：
  - 文章列表增加“图片本地化状态”：待处理、处理中、完成、部分失败。
  - 失败时保留原图链接，不破坏文章内容。

## P2：服务器日志过于 verbose

- 现象：线上日志大量输出 MyBatis SQL、用户记录、文章内容片段。
- 风险：
  - 日志体积变大。
  - 服务器日志中会出现手机号、邮箱、文章正文等敏感或半敏感信息。
- 建议：
  - 生产环境关闭 MyBatis SQL 明细日志。
  - 仅保留必要错误、慢接口、关键业务日志。

## P2：后台入口路径在公开仓库中可见

- 现象：`README.md`、`AGENTS.md`、Nginx 配置中包含 `/boylu1107` 后台入口。
- 风险：这不是密码，但会降低隐藏后台入口的意义。
- 建议：
  - 公开文档中改成 `/admin-secret-path/` 示例。
  - 实际路径仅保留在服务器 Nginx 配置或私有部署说明中。

## P3：前端构建仍有体积和 Sass 警告

- 现象：后台构建仍有 Sass legacy API 和大 chunk 警告。
- 影响：暂不影响运行。
- 建议：后续单独做依赖拆分和 Sass API 升级。

## P3：仓库仍包含旧项目与历史 SQL

- 现象：仓库中仍有 `uniapp-blog`、`mj-blog.sql`、旧命名遗留内容。
- 风险：
  - 新用户拉取后不容易判断当前有效模块。
  - 历史 SQL 可能包含不适合公开的数据结构或初始化账号信息。
- 建议：
  - README 明确“当前有效模块”。
  - 检查 SQL 是否包含真实数据，必要时替换为脱敏初始化脚本。

## 本次巡检结论

当前最优先要修的是 Notion 同步保护：不能让空正文同步覆盖已有文章。其次处理百度推送 SSL、`ip2region.xdb` 权限和线上日志降噪。
